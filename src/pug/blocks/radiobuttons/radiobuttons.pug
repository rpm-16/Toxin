mixin radiobuttons(heading, args)
    - let head = heading;
    - let arg = args;

    .form-group
        label.form-group_wrapper
            .form-group__title= head.groupHeader
            if head.note
            .form-group__note= head.note

        .radiobuttons-group
            ul.radiobuttons-group__list
                for t in arg
                    li.radiobuttons__list__item
                        input(type= head.type)(checked= t.checked)
                        .radiobuttons__list__label= t.label
                    