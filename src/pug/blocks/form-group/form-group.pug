mixin form-group(args)
  - let objArgs = args
    
    .form-group
      label.form-group__wrapper
        .form-group__title= objArgs.title
        if objArgs.note
          .form-group__note= objArgs.note
        
    block